# EPUB 3 NAV fixture

Self-authored and redistributable. This transparent fixture covers an OPF in a
non-default Unicode path, percent-encoded and Unicode hrefs, nested EPUB 3 NAV,
a matching NCX alternate, a non-linear spine item, local CSS/image resources,
and active or remote content that the preview must block.
