# Architecture

Status: Phase 0 baseline.

The architecture must keep the original EPUB archive and source text authoritative. Preview DOM is never a save source. Phase 0 will select and document the ZIP writer and the loss-minimizing XML text-token patch strategy only after executable preservation tests pass.

The intended module boundaries are described in `product-requirements.md`. This file must be expanded with actual decisions and evidence as the spikes are implemented.
